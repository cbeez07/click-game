import React from 'react';

const Header = () => (
    <div className="jumbotron">
        <h1 className="display-4 text-center">Clicky Game!</h1>
        <h2 className="display-6 text-center">Click on an image to earn points, but don't click on any more than once!</h2>
    </div>
);

export default Header;